
import Position from '../dna/position';

export default class ProteinVariant extends Position {
  // position
  // wild type / alt. type
  // protein consequence: del, dup,...
  // protein ref.
  // 
}
